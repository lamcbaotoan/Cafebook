<Page x:Class="Cafebook.Views.nhanvien.pages.ChamCongView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      mc:Ignorable="d" 
      d:DesignHeight="650" d:DesignWidth="1050"
      Title="ChamCongView" FontFamily="Segoe UI" Loaded="Page_Loaded">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Background="WhiteSmoke" Padding="20" Margin="10">
            <StackPanel HorizontalAlignment="Center">
                <TextBlock x:Name="lblThoiGian" Text="15/09/2025 - 21:34:02" FontSize="32" FontWeight="Bold"/>
                <TextBlock x:Name="lblCaLamViec" Text="Hôm nay: Ca Chiều (15:00 - 23:00)" HorizontalAlignment="Center" FontSize="16" Margin="0,5,0,15"/>
                <StackPanel Orientation="Horizontal">
                    <Button x:Name="btnVaoCa" Content="VÀO CA" Width="150" Height="60" FontSize="20" FontWeight="Bold" Background="#27AE60" Foreground="White" Margin="10" Click="BtnVaoCa_Click"/>
                    <Button x:Name="btnRaCa" Content="RA CA" Width="150" Height="60" FontSize="20" FontWeight="Bold" Background="#E74C3C" Foreground="White" Margin="10" Click="BtnRaCa_Click"/>
                </StackPanel>
                <TextBlock x:Name="lblTrangThaiChamCong" Text="Trạng thái: Chưa vào ca" HorizontalAlignment="Center" FontSize="14" Margin="0,10,0,0" FontStyle="Italic"/>
            </StackPanel>
        </Border>

        <GroupBox Grid.Row="1" Header="Lịch sử chấm công của bạn" Margin="10">
            <DataGrid x:Name="dgLichSuChamCong" IsReadOnly="True" AutoGenerateColumns="False">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Ngày làm" Binding="{Binding NgayLam, StringFormat='dd/MM/yyyy'}" Width="*"/>
                    <DataGridTextColumn Header="Ca làm" Binding="{Binding TenCa}" Width="*"/>
                    <DataGridTextColumn Header="Giờ vào" Binding="{Binding GioVao, StringFormat='HH:mm:ss'}" Width="*"/>
                    <DataGridTextColumn Header="Giờ ra" Binding="{Binding GioRa, StringFormat='HH:mm:ss'}" Width="*"/>
                    <DataGridTextColumn Header="Tổng giờ" Binding="{Binding SoGioLam, StringFormat=N2}" Width="*"/>
                </DataGrid.Columns>
            </DataGrid>
        </GroupBox>
    </Grid>
</Page>