<Window x:Class="Cafebook.Views.nhanvien.ManHinhNhanVien"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Cafebook.Views.nhanvien"
        mc:Ignorable="d"
        Title="Cafebook - Nhân viên" Height="720" Width="1280"
        WindowStartupLocation="CenterScreen"
        FontFamily="Segoe UI">

    <Window.Resources>
        <Style x:Key="NavButton" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="#D5D8DC"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="HorizontalContentAlignment" Value="Left"/>
            <Setter Property="Padding" Value="20,15"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Left" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#4A5A6A"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="220"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <Grid Grid.Column="0" Background="#2C3E50">
            <StackPanel>
                <TextBlock Text="Cafebook" FontSize="24" FontWeight="Bold" Foreground="White" 
                           HorizontalAlignment="Center" Margin="0,20,0,30"/>

                <TextBlock Text="Nghiệp vụ Bán hàng" Foreground="#85929E" Margin="20,10,0,5" FontSize="12" FontWeight="Bold"/>
                <Button x:Name="btnSoDoBan" Content="Sơ đồ bàn" Style="{StaticResource NavButton}" Click="BtnSoDoBan_Click"/>

                <TextBlock Text="Quản lý chung" Foreground="#85929E" Margin="20,20,0,5" FontSize="12" FontWeight="Bold"/>
                <Button x:Name="btnDatBanSach" Content="Quản lý Đặt bàn &amp; Sách" Style="{StaticResource NavButton}" Click="BtnDatBanSach_Click"/>

                <TextBlock Text="Tài khoản" Foreground="#85929E" Margin="20,20,0,5" FontSize="12" FontWeight="Bold"/>
                <Button x:Name="btnThongTinCaNhan" Content="Thông tin cá nhân" Style="{StaticResource NavButton}" Click="BtnThongTinCaNhan_Click"/>
                <Button x:Name="btnChamCong" Content="Chấm công" Style="{StaticResource NavButton}" Click="BtnChamCong_Click"/>
            </StackPanel>

            <Button x:Name="btnDangXuat" Content="Đăng xuất" VerticalAlignment="Bottom" Height="50" 
                    Background="#E74C3C" Foreground="White" BorderThickness="0" FontSize="14"
                    Click="BtnDangXuat_Click"/>
        </Grid>

        <Frame x:Name="MainFrame" Grid.Column="1" NavigationUIVisibility="Hidden" Background="#F4F6F7"/>

    </Grid>
</Window>