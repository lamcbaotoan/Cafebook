<Window x:Class="Cafebook.Views.Common.ThongBaoChiTietWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Danh sách Thông báo Chưa đọc" Height="500" Width="600"
        WindowStartupLocation="CenterOwner" WindowStyle="ToolWindow">
    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <ScrollViewer Grid.Row="0" VerticalScrollBarVisibility="Auto">
            <ItemsControl x:Name="icThongBao">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border Background="#F9F9F9" CornerRadius="5" Padding="15" Margin="0,0,0,10">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>

                                <StackPanel Grid.Column="0">
                                    <TextBlock Text="{Binding NoiDung}" TextWrapping="Wrap" FontSize="14"/>
                                    <TextBlock Margin="0,5,0,0" FontStyle="Italic" FontSize="12" Foreground="Gray">
                                        <Run Text="Gửi bởi:"/>
                                        <Run Text="{Binding TenNhanVien}" FontWeight="SemiBold"/>
                                        <Run Text=" - lúc "/>
                                        <Run Text="{Binding ThoiGianTao, StringFormat='HH:mm dd/MM/yyyy'}"/>
                                    </TextBlock>
                                </StackPanel>

                                <Button Grid.Column="1" VerticalAlignment="Center" Margin="10,0,0,0"
                                        Content="Đánh dấu đã đọc" Padding="8,4"
                                        x:Name="btnDanhDauDaDocItem" Click="BtnDanhDauDaDocItem_Click"/>
                            </Grid>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>

        <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,0">
            <Button Content="Đánh dấu tất cả đã đọc" Margin="0,0,10,0" Click="BtnDanhDauTatCaDaDoc_Click"/>
            <Button Content="Đóng" IsCancel="True"/>
        </StackPanel>
    </Grid>
</Window>