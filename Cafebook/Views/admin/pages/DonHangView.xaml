<Page x:Class="Cafebook.Views.admin.pages.DonHangView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:Cafebook.Views.admin.pages"
      mc:Ignorable="d" 
      d:DesignHeight="700" d:DesignWidth="1100"
      Title="DonHangView" FontFamily="Segoe UI" Loaded="Page_Loaded">

    <Page.Resources>
        <SolidColorBrush x:Key="PrimaryBrownBrush" Color="#6F4E37"/>
        <SolidColorBrush x:Key="DarkBrownBrush" Color="#4E342E"/>
        <SolidColorBrush x:Key="LightCreamBrush" Color="#FFF8E1"/>
        <SolidColorBrush x:Key="WhiteTextBrush" Color="#FFFFFF"/>
        <SolidColorBrush x:Key="TextGrayBrush" Color="#757575"/>
        <SolidColorBrush x:Key="BorderGrayBrush" Color="#E0E0E0"/>
        <SolidColorBrush x:Key="ActionGreenBrush" Color="#66BB6A"/>
        <SolidColorBrush x:Key="ActionRedBrush" Color="#EF5350"/>
        <SolidColorBrush x:Key="ActionBlueBrush" Color="#42A5F5"/>

        <Geometry x:Key="IconFilter">M14,12V19.88C14.04,20.18 13.94,20.5 13.71,20.71C13.32,21.1 12.69,21.1 12.3,20.71L10.29,18.7C10.06,18.47 9.96,18.16 10,17.87V12H14M15,9H9V4H15V9Z</Geometry>
        <Geometry x:Key="IconFilterOff">M18.84,13H21.84C21.43,18.28 17.06,22.25 11.84,21.84C7.43,21.5 3.76,18.12 2.68,13.5L5.68,13.5C6.44,16.5 9.17,18.88 12.34,18.91C16.14,19 19.17,16 18.84,13M10.16,3H1.16C1.57,3.72 2.06,4.39 2.6,5L6.4,5C6.67,4.32 6.98,3.67 7.33,3H10.16M11.66,3.09L13.16,4.59L17.34,8.78L19,10.44L17.59,11.85L16.29,10.55L14.28,8.54L10.96,5.22C10.61,4.95 10.38,4.55 10.28,4.11C10.18,3.68 10.16,3.37 10.16,3.09M22.84,8.5C22.42,3.22 18.05,-0.75 12.84,-1.16C12.42,-1.2 12,-1.18 11.6,-1.13L13.1,0.37L16.2,3.47L19.49,6.76C19.84,7.03 20.07,7.43 20.17,7.87C20.27,8.3 20.29,8.61 20.29,8.89H22.84Z</Geometry>
        <Geometry x:Key="IconPrint">M19,8H5C3.34,8 2,9.34 2,11V17H6V21H18V17H22V11C22,9.34 20.66,8 19,8M16,19H8V14H16V19M19,12C18.45,12 18,11.55 18,11C18,10.45 18.45,10 19,10C19.55,10 20,10.45 20,11C20,11.55 19.55,12 19,12M18,3H6V7H18V3Z</Geometry>
        <Geometry x:Key="IconCancel">M19,6.41L17.59,5L12,10.59L6.41,5L5,6.41L10.59,12L5,17.59L6.41,19L12,13.41L17.59,19L19,17.59L13.41,12L19,6.41Z</Geometry>

        <Style x:Key="PrimaryButton" TargetType="Button">
            <Setter Property="Background" Value="{StaticResource PrimaryBrownBrush}"/>
            <Setter Property="Foreground" Value="{StaticResource WhiteTextBrush}"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Padding" Value="12,8"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" CornerRadius="4" Background="{TemplateBinding Background}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="{StaticResource DarkBrownBrush}"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Opacity" Value="0.5"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

    </Page.Resources>

    <Grid Background="{StaticResource LightCreamBrush}" x:Name="MainPanel" >
        <Grid.RenderTransform>
            <TranslateTransform Y="20"/>
        </Grid.RenderTransform>
        <Grid.Triggers>
            <EventTrigger RoutedEvent="Loaded">
                <BeginStoryboard>
                    <Storyboard>
                        <DoubleAnimation Storyboard.TargetProperty="Opacity" From="0" To="1" Duration="0:0:0.5"/>
                        <DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(TranslateTransform.Y)" To="0" Duration="0:0:0.5">
                            <DoubleAnimation.EasingFunction>
                                <BackEase EasingMode="EaseOut" Amplitude="0.2"/>
                            </DoubleAnimation.EasingFunction>
                        </DoubleAnimation>
                    </Storyboard>
                </BeginStoryboard>
            </EventTrigger>
        </Grid.Triggers>

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Background="{StaticResource WhiteTextBrush}" CornerRadius="8" Padding="15" Margin="15,15,15,0">
            <Border.Effect>
                <DropShadowEffect ShadowDepth="1" Color="#000" Opacity="0.08" BlurRadius="20"/>
            </Border.Effect>
            <StackPanel Orientation="Horizontal" Margin="0,-5,0,-5">
                <TextBlock Text="Từ ngày:" VerticalAlignment="Center" FontWeight="SemiBold" Foreground="{StaticResource DarkBrownBrush}"/>
                <DatePicker x:Name="dpTuNgay" Width="130" Margin="10,0,0,0" VerticalContentAlignment="Center"/>
                <TextBlock Text="Đến ngày:" VerticalAlignment="Center" FontWeight="SemiBold" Foreground="{StaticResource DarkBrownBrush}" Margin="20,0,0,0"/>
                <DatePicker x:Name="dpDenNgay" Width="130" Margin="10,0,0,0" VerticalContentAlignment="Center"/>
                <TextBlock Text="Trạng thái:" VerticalAlignment="Center" FontWeight="SemiBold" Foreground="{StaticResource DarkBrownBrush}" Margin="20,0,0,0"/>
                <ComboBox x:Name="cmbTrangThai" Width="150" Margin="10,0,0,0" VerticalContentAlignment="Center">
                    <ComboBoxItem Content="Tất cả"/>
                    <ComboBoxItem Content="Chưa thanh toán"/>
                    <ComboBoxItem Content="Đã thanh toán"/>
                </ComboBox>
                <Button x:Name="btnLoc" Margin="20,0,0,0" Style="{StaticResource PrimaryButton}" Click="BtnLoc_Click">
                    <Grid Width="69">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <Viewbox Grid.Column="0" Width="14" Height="14" Margin="0,0,8,0">
                            <Path Data="{StaticResource IconFilter}" Fill="{Binding Foreground, RelativeSource={RelativeSource AncestorType=Button}}"/>
                        </Viewbox>
                        <TextBlock Grid.Column="1" Text="Lọc" VerticalAlignment="Center" Margin="7,0,-7,0"/>
                    </Grid>
                </Button>
                <Button x:Name="btnXoaLoc" Margin="8,0,0,0" Style="{StaticResource PrimaryButton}" Background="White" Foreground="{StaticResource TextGrayBrush}" BorderBrush="{StaticResource BorderGrayBrush}" BorderThickness="1" Click="BtnXoaLoc_Click">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <Viewbox Grid.Column="0" Width="14" Height="14" Margin="4,3,4,3">
                            <Path Data="{StaticResource IconFilterOff}" Fill="{Binding Foreground, RelativeSource={RelativeSource AncestorType=Button}}"/>
                        </Viewbox>
                        <TextBlock Grid.Column="1" Text="Xóa bộ lọc" VerticalAlignment="Center" Margin="0,1,0,0"/>
                    </Grid>
                </Button>
            </StackPanel>
        </Border>

        <Grid Grid.Row="1" Margin="15">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="2*"/>
                <ColumnDefinition Width="1*"/>
            </Grid.ColumnDefinitions>

            <DataGrid Grid.Column="0" x:Name="dgDonHang" AutoGenerateColumns="False" IsReadOnly="True" SelectionMode="Single" SelectionChanged="DgDonHang_SelectionChanged">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Mã HĐ" Binding="{Binding IdHoaDon}" Width="60"/>
                    <DataGridTextColumn Header="Thời gian tạo" Binding="{Binding ThoiGianTao, StringFormat='dd/MM HH:mm'}" Width="120"/>
                    <DataGridTextColumn Header="Nhân viên" Binding="{Binding TenNhanVien}" Width="*"/>
                    <DataGridTextColumn Header="Bàn" Binding="{Binding SoBan}" Width="80"/>
                    <DataGridTextColumn Header="Thành tiền" Binding="{Binding ThanhTien, StringFormat=N0}" Width="100"/>
                    <DataGridTextColumn Header="Trạng thái" Binding="{Binding TrangThai}" Width="120"/>
                </DataGrid.Columns>
            </DataGrid>

            <Border Grid.Column="1" Background="{StaticResource WhiteTextBrush}" CornerRadius="8" Margin="15,0,0,0" Padding="20">
                <Border.Effect>
                    <DropShadowEffect ShadowDepth="1" Color="#000" Opacity="0.08" BlurRadius="20"/>
                </Border.Effect>
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    <TextBlock Text="Chi tiết Đơn hàng" FontSize="18" FontWeight="Bold" Foreground="{StaticResource DarkBrownBrush}" Margin="0,0,0,15"/>
                    <DataGrid Grid.Row="1" x:Name="dgChiTietDonHang" AutoGenerateColumns="False" IsReadOnly="True" Background="Transparent" BorderThickness="0">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="Sản phẩm" Binding="{Binding TenSanPham}" Width="*"/>
                            <DataGridTextColumn Header="SL" Binding="{Binding SoLuong}" Width="40"/>
                            <DataGridTextColumn Header="Thành tiền" Binding="{Binding ThanhTien, StringFormat=N0}" Width="80"/>
                        </DataGrid.Columns>
                    </DataGrid>
                    <StackPanel Grid.Row="2" Margin="0,15,0,0">
                        <Button x:Name="btnInLaiHoaDon" Style="{StaticResource PrimaryButton}" Background="{StaticResource ActionBlueBrush}" IsEnabled="False" Click="BtnInLaiHoaDon_Click" Height="40">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <Viewbox Grid.Column="0" Width="14" Height="14" Margin="0,0,8,0">
                                    <Path Data="{StaticResource IconPrint}" Fill="{Binding Foreground, RelativeSource={RelativeSource AncestorType=Button}}"/>
                                </Viewbox>
                                <TextBlock Grid.Column="1" Text="In lại Hóa đơn" VerticalAlignment="Center" Margin="0,1,0,0"/>
                            </Grid>
                        </Button>
                        <Button x:Name="btnHuyDonHang" Margin="0,8,0,0" Style="{StaticResource PrimaryButton}" Background="{StaticResource ActionRedBrush}" IsEnabled="False" Click="BtnHuyDonHang_Click" Height="35">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <Viewbox Grid.Column="0" Width="14" Height="14" Margin="0,0,8,0">
                                    <Path Data="{StaticResource IconCancel}" Fill="{Binding Foreground, RelativeSource={RelativeSource AncestorType=Button}}"/>
                                </Viewbox>
                                <TextBlock Grid.Column="1" Text="Hủy Đơn hàng" VerticalAlignment="Center" Margin="0,1,0,0"/>
                            </Grid>
                        </Button>
                    </StackPanel>
                </Grid>
            </Border>
        </Grid>
    </Grid>
</Page>