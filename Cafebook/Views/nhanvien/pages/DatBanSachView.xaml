<Page x:Class="Cafebook.Views.nhanvien.pages.DatBanSachView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:Cafebook.Views.nhanvien.pages"
      mc:Ignorable="d" 
      d:DesignHeight="700" d:DesignWidth="1100"
      Title="DatBanSachView" FontFamily="Segoe UI" Loaded="Page_Loaded">

    <Page.Resources>
        <SolidColorBrush x:Key="PrimaryBrownBrush" Color="#6F4E37"/>
        <SolidColorBrush x:Key="DarkBrownBrush" Color="#4E342E"/>
        <SolidColorBrush x:Key="AccentOrangeBrush" Color="#D27D2D"/>
        <SolidColorBrush x:Key="LightCreamBrush" Color="#FFF8E1"/>
        <SolidColorBrush x:Key="WhiteTextBrush" Color="#FFFFFF"/>
        <SolidColorBrush x:Key="TextGrayBrush" Color="#757575"/>
        <SolidColorBrush x:Key="BorderGrayBrush" Color="#E0E0E0"/>
        <SolidColorBrush x:Key="ActionGreenBrush" Color="#66BB6A"/>
        <SolidColorBrush x:Key="ActionRedBrush" Color="#EF5350"/>
        <Geometry x:Key="IconSearch">M9.5,3A6.5,6.5 0 0,1 16,9.5C16,11.11 15.41,12.59 14.44,13.73L14.71,14H15.5L20.5,19L19,20.5L14,15.5V14.71L13.73,14.44C12.59,15.41 11.11,16 9.5,16A6.5,6.5 0 0,1 3,9.5A6.5,6.5 0 0,1 9.5,3M9.5,5C7,5 5,7 5,9.5C5,12 7,14 9.5,14C12,14 14,12 14,9.5C14,7 12,5 9.5,5Z</Geometry>
        <Geometry x:Key="IconAdd">M19,13H13V19H11V13H5V11H11V5H13V11H19V13Z</Geometry>
        <Geometry x:Key="IconCheck">M9,20.42L2.79,14.21L4.21,12.79L9,17.58L19.79,6.79L21.21,8.21L9,20.42Z</Geometry>
        <Geometry x:Key="IconCancel">M19,6.41L17.59,5L12,10.59L6.41,5L5,6.41L10.59,12L5,17.59L6.41,19L12,13.41L17.59,19L19,17.59L13.41,12L19,6.41Z</Geometry>
        <Style x:Key="IconButton" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="8"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="20" Padding="{TemplateBinding Padding}">
                            <Path Data="{Binding Content, RelativeSource={RelativeSource TemplatedParent}}" 
                                  Fill="{Binding Foreground, RelativeSource={RelativeSource TemplatedParent}}" 
                                  Width="14" Height="14" Stretch="Uniform"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#E0E0E0"/>
                </Trigger>
            </Style.Triggers>
        </Style>
        <Style x:Key="PrimaryButton" TargetType="Button">
            <Setter Property="Background" Value="{StaticResource PrimaryBrownBrush}"/>
            <Setter Property="Foreground" Value="{StaticResource WhiteTextBrush}"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Padding" Value="12,8"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" CornerRadius="4" Background="{TemplateBinding Background}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="{StaticResource DarkBrownBrush}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style x:Key="ModernTabItem" TargetType="TabItem">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TabItem">
                        <Border x:Name="border" BorderThickness="0,0,0,3" BorderBrush="Transparent" Padding="15,10">
                            <TextBlock Text="{TemplateBinding Header}" FontSize="16" FontWeight="SemiBold" Foreground="{StaticResource TextGrayBrush}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter Property="Foreground" Value="{StaticResource PrimaryBrownBrush}"/>
                                <Setter TargetName="border" Property="BorderBrush" Value="{StaticResource AccentOrangeBrush}"/>
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Foreground" Value="{StaticResource DarkBrownBrush}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Page.Resources>

    <!-- Áp dụng animation cho Grid gốc này -->
    <Grid Background="{StaticResource LightCreamBrush}" x:Name="MainPanel" >
        <Grid.RenderTransform>
            <TranslateTransform Y="20"/>
        </Grid.RenderTransform>
        <Grid.Triggers>
            <EventTrigger RoutedEvent="Loaded">
                <BeginStoryboard>
                    <Storyboard>
                        <DoubleAnimation Storyboard.TargetProperty="Opacity" From="0" To="1" Duration="0:0:0.5"/>
                        <DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(TranslateTransform.Y)" To="0" Duration="0:0:0.5">
                            <DoubleAnimation.EasingFunction>
                                <BackEase EasingMode="EaseOut" Amplitude="0.2"/>
                            </DoubleAnimation.EasingFunction>
                        </DoubleAnimation>
                    </Storyboard>
                </BeginStoryboard>
            </EventTrigger>
        </Grid.Triggers>

        <!-- Toàn bộ nội dung gốc được giữ nguyên -->
        <TabControl BorderThickness="0" Background="Transparent">
            <TabControl.ItemContainerStyle>
                <Style TargetType="TabItem" BasedOn="{StaticResource ModernTabItem}"/>
            </TabControl.ItemContainerStyle>

            <TabItem Header="Quản lý Đặt Bàn">
                <Grid Margin="15">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="360"/>
                    </Grid.ColumnDefinitions>

                    <StackPanel Grid.Column="0" Margin="0,0,15,0">
                        <Border Background="{StaticResource WhiteTextBrush}" CornerRadius="8" Padding="15">
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="Xem phiếu ngày:" VerticalAlignment="Center" FontWeight="SemiBold" Foreground="{StaticResource DarkBrownBrush}"/>
                                <DatePicker x:Name="dpChonNgayDatBan" Width="150" Margin="10,0,0,0" SelectedDateChanged="DpChonNgayDatBan_SelectedDateChanged" VerticalContentAlignment="Center"/>
                                <Button x:Name="btnHomNay" Content="Hôm nay" Margin="5,0,0,0" Padding="8,4" Click="BtnHomNay_Click"/>
                                <TextBlock Text="Tìm kiếm (Tên/SĐT):" VerticalAlignment="Center" FontWeight="SemiBold" Foreground="{StaticResource DarkBrownBrush}" Margin="25,0,0,0"/>
                                <TextBox x:Name="txtTimKiemDatBan" Width="200" VerticalAlignment="Center" Margin="10,0,0,0" Padding="5" TextChanged="TxtTimKiemDatBan_TextChanged"/>
                            </StackPanel>
                        </Border>

                        <DataGrid Margin="0,15,0,0" x:Name="dgPhieuDatBan" AutoGenerateColumns="False" IsReadOnly="True" SelectionMode="Single">
                            <DataGrid.Columns>
                                <DataGridTextColumn Header="ID" Binding="{Binding IdPhieuDatBan}" Width="50"/>
                                <DataGridTextColumn Header="Tên Khách" Binding="{Binding TenKhachHangHienThi}" Width="*"/>
                                <DataGridTextColumn Header="SĐT" Binding="{Binding SdtHienThi}" Width="120"/>
                                <DataGridTextColumn Header="Bàn" Binding="{Binding SoBanHienThi}" Width="80"/>
                                <DataGridTextColumn Header="Thời gian" Binding="{Binding ThoiGianDat, StringFormat='HH:mm dd/MM'}" Width="120"/>
                                <DataGridTextColumn Header="SL Khách" Binding="{Binding SoLuongKhach}" Width="80"/>
                                <DataGridTextColumn Header="Trạng thái" Binding="{Binding TrangThai}" Width="100"/>
                            </DataGrid.Columns>
                        </DataGrid>

                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,15,0,0">
                            <Button x:Name="btnKhachDaDen" Style="{StaticResource PrimaryButton}" Background="{StaticResource ActionGreenBrush}" Click="BtnKhachDaDen_Click">
                                <Grid Height="28">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="*"/>
                                    </Grid.ColumnDefinitions>
                                    <Viewbox Grid.Column="0" Width="14" Height="14" Margin="0,0,8,0">
                                        <Path Data="{StaticResource IconCheck}" Fill="{Binding Foreground, RelativeSource={RelativeSource AncestorType=Button}}"/>
                                    </Viewbox>
                                    <TextBlock Text="Khách đã đến" VerticalAlignment="Center" Margin="19,0,3,0" Grid.ColumnSpan="2"/>
                                </Grid>
                            </Button>
                            <Button x:Name="btnHuyPhieuDat" Style="{StaticResource PrimaryButton}" Background="{StaticResource ActionRedBrush}" Margin="10,0,0,0" Click="BtnHuyPhieuDat_Click" Height="36">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="*"/>
                                    </Grid.ColumnDefinitions>
                                    <Viewbox Grid.Column="0" Width="14" Height="14" Margin="0,0,8,0">
                                        <Path Data="{StaticResource IconCancel}" Fill="{Binding Foreground, RelativeSource={RelativeSource AncestorType=Button}}"/>
                                    </Viewbox>
                                    <TextBlock Text="Hủy Phiếu Đặt" VerticalAlignment="Center" Margin="19,0,3,0" Grid.ColumnSpan="2"/>
                                </Grid>
                            </Button>
                        </StackPanel>
                    </StackPanel>

                    <Border Grid.Column="1" Background="{StaticResource WhiteTextBrush}" CornerRadius="8" Padding="20">
                        <ScrollViewer VerticalScrollBarVisibility="Auto">
                            <StackPanel>
                                <TextBlock Text="Tạo Phiếu Đặt Bàn Mới" FontSize="18" FontWeight="Bold" Foreground="{StaticResource DarkBrownBrush}" Margin="0,0,0,15"/>
                                <TextBlock Text="Tên khách hàng:" Margin="0,0,0,5"/>
                                <TextBox x:Name="txtTenKhachDat" Margin="0,0,0,10" Padding="5"/>
                                <TextBlock Text="Số điện thoại:" Margin="0,0,0,5"/>
                                <TextBox x:Name="txtSdtKhachDat" Margin="0,0,0,10" Padding="5"/>
                                <TextBlock Text="Ngày đặt:" Margin="0,0,0,5"/>
                                <DatePicker x:Name="dpNgayDatMoi" Margin="0,0,0,10" Padding="5"/>
                                <TextBlock Text="Thời gian đặt (giờ:phút):" Margin="0,0,0,5"/>
                                <TextBox x:Name="txtGioPhutDat" Text="19:00" Margin="0,0,0,10" Padding="5"/>
                                <TextBlock Text="Số lượng khách:" Margin="0,0,0,5"/>
                                <TextBox x:Name="txtSoLuongKhach" Margin="0,0,0,10" Padding="5"/>
                                <TextBlock Text="Chọn bàn:" Margin="0,0,0,5"/>
                                <ComboBox x:Name="cmbBan" DisplayMemberPath="SoBan" SelectedValuePath="IdBan" Margin="0,0,0,10" Padding="5"/>
                                <TextBlock Text="Ghi chú:" Margin="0,0,0,5"/>
                                <TextBox x:Name="txtGhiChuDat" Margin="0,0,0,10" Padding="5" Height="60" TextWrapping="Wrap" AcceptsReturn="True" VerticalScrollBarVisibility="Auto"/>
                                <Button x:Name="btnTaoPhieuDat" Margin="0,10,0,0" Style="{StaticResource PrimaryButton}" Click="BtnTaoPhieuDat_Click" Height="36">
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="Auto"/>
                                            <ColumnDefinition Width="*"/>
                                        </Grid.ColumnDefinitions>
                                        <Viewbox Grid.Column="0" Width="14" Height="14" Margin="0,0,8,0">
                                            <Path Data="{StaticResource IconAdd}" Fill="{Binding Foreground, RelativeSource={RelativeSource AncestorType=Button}}"/>
                                        </Viewbox>
                                        <TextBlock Grid.Column="1" Text="Tạo Phiếu Đặt" VerticalAlignment="Center"/>
                                    </Grid>
                                </Button>
                            </StackPanel>
                        </ScrollViewer>
                    </Border>
                </Grid>
            </TabItem>


            <TabItem Header="Quản lý Thuê Sách">
                <Grid Margin="15">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="360"/>
                    </Grid.ColumnDefinitions>

                    <Grid Grid.Column="0" Margin="0,0,15,0">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>
                        <Border Grid.Row="0" Background="{StaticResource WhiteTextBrush}" CornerRadius="8" Padding="15" Margin="0,0,0,15">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <TextBox x:Name="txtTimKiemPhieuTra" Grid.Column="0" VerticalContentAlignment="Center" Padding="5" TextChanged="TxtTimKiemPhieuTra_TextChanged"/>
                                <Button Grid.Column="1" x:Name="btnXoaTimKiemPhieuTra" Margin="10,0,0,0" Style="{StaticResource IconButton}" Content="{StaticResource IconCancel}" Foreground="{StaticResource TextGrayBrush}" Click="BtnXoaTimKiemPhieuTra_Click"/>
                            </Grid>
                        </Border>
                        <GroupBox Grid.Row="1" Header="Sách đang được thuê">
                            <DataGrid x:Name="dgPhieuDangThue" AutoGenerateColumns="False" IsReadOnly="True" SelectionMode="Single" SelectionChanged="DgPhieu_SelectionChanged">
                                <DataGrid.Columns>
                                    <DataGridTextColumn Header="Mã Phiếu" Binding="{Binding IdPhieuThue}" Width="80"/>
                                    <DataGridTextColumn Header="Tên Sách" Binding="{Binding TieuDeSach}" Width="*"/>
                                    <DataGridTextColumn Header="Khách Hàng" Binding="{Binding TenKhachHang}" Width="150"/>
                                    <DataGridTextColumn Header="Ngày Thuê" Binding="{Binding NgayThue, StringFormat='dd/MM/yyyy'}" Width="100"/>
                                    <DataGridTextColumn Header="Hẹn Trả" Binding="{Binding NgayHenTra, StringFormat='dd/MM/yyyy'}" Width="100"/>
                                </DataGrid.Columns>
                            </DataGrid>
                        </GroupBox>
                        <GridSplitter Grid.Row="2" Height="5" HorizontalAlignment="Stretch" Background="Transparent"/>
                        <GroupBox Grid.Row="3" Header="Sách đã quá hạn">
                            <DataGrid x:Name="dgPhieuQuaHan" AutoGenerateColumns="False" IsReadOnly="True" SelectionMode="Single" SelectionChanged="DgPhieu_SelectionChanged">
                                <DataGrid.Columns>
                                    <DataGridTextColumn Header="Mã Phiếu" Binding="{Binding IdPhieuThue}" Width="80"/>
                                    <DataGridTextColumn Header="Tên Sách" Binding="{Binding TieuDeSach}" Width="*"/>
                                    <DataGridTextColumn Header="Khách Hàng" Binding="{Binding TenKhachHang}" Width="150"/>
                                    <DataGridTextColumn Header="Hẹn Trả" Binding="{Binding NgayHenTra, StringFormat='dd/MM/yyyy'}" Width="100"/>
                                    <DataGridTextColumn Header="Trạng thái" Binding="{Binding TrangThai}" Width="100"/>
                                </DataGrid.Columns>
                            </DataGrid>
                        </GroupBox>
                    </Grid>

                    <Border Grid.Column="1" Background="{StaticResource WhiteTextBrush}" CornerRadius="8" Padding="20">
                        <ScrollViewer VerticalScrollBarVisibility="Auto">
                            <StackPanel>
                                <GroupBox Header="Cho Mượn Sách">
                                    <StackPanel Margin="5">
                                        <CheckBox x:Name="chkKhachHangMoi" Content="Khách hàng mới" Margin="0,0,0,10" Checked="ChkKhachHangMoi_Changed" Unchecked="ChkKhachHangMoi_Changed"/>
                                        <StackPanel x:Name="panelKhachCu">
                                            <TextBlock Text="Tìm khách hàng (Tên/SĐT):" Margin="0,0,0,5"/>
                                            <TextBox x:Name="txtTimKiemKhachHang" Margin="0,0,0,5" Padding="5" TextChanged="TxtTimKiemKhachHang_TextChanged"/>
                                            <TextBlock Text="Chọn khách hàng thành viên:" Margin="0,0,0,5"/>
                                            <ComboBox x:Name="cmbKhachHang_Thue" DisplayMemberPath="HoTen" SelectedValuePath="IdKhachHang" Margin="0,0,0,10" Padding="5"/>
                                        </StackPanel>
                                        <StackPanel x:Name="panelKhachMoi" Visibility="Collapsed">
                                            <TextBlock Text="Tên khách hàng mới:" Margin="0,0,0,5"/>
                                            <TextBox x:Name="txtTenKhachMoi" Margin="0,0,0,5" Padding="5"/>
                                            <TextBlock Text="SĐT khách hàng mới:" Margin="0,0,0,5"/>
                                            <TextBox x:Name="txtSdtKhachMoi" Margin="0,0,0,10" Padding="5"/>
                                        </StackPanel>

                                        <TextBlock Text="Tìm sách (Tên, Tác giả, Vị trí):" Margin="0,10,0,5"/>
                                        <TextBox x:Name="txtTimKiemSach" Margin="0,0,0,5" Padding="5" TextChanged="TxtTimKiemSach_TextChanged"/>

                                        <TextBlock Text="Chọn sách muốn thuê:" Margin="0,0,0,5"/>
                                        <ComboBox x:Name="cmbSach_Thue" DisplayMemberPath="TieuDe" SelectedValuePath="IdSach" Margin="0,0,0,10" Padding="5"/>
                                        <TextBlock Text="Ngày hẹn trả:" Margin="0,0,0,5"/>
                                        <DatePicker x:Name="dpNgayHenTra" Margin="0,0,0,10" Padding="5"/>
                                        <Button x:Name="btnXacNhanChoMuon" Style="{StaticResource PrimaryButton}" FontWeight="Bold" Click="BtnXacNhanChoMuon_Click" Height="26">
                                            <Grid>
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="Auto"/>
                                                    <ColumnDefinition Width="*"/>
                                                </Grid.ColumnDefinitions>
                                                <Viewbox Grid.Column="0" Width="14" Height="14" Margin="0,0,8,0">
                                                    <Path Data="{StaticResource IconAdd}" Fill="{Binding Foreground, RelativeSource={RelativeSource AncestorType=Button}}"/>
                                                </Viewbox>
                                                <TextBlock Grid.Column="1" Text="XÁC NHẬN CHO MƯỢN" VerticalAlignment="Center" Margin="0,1,0,0"/>
                                            </Grid>
                                        </Button>
                                    </StackPanel>
                                </GroupBox>
                                <GroupBox Header="Nhận Trả Sách" Margin="0,15,0,0" x:Name="gbTraSach" IsEnabled="False">
                                    <StackPanel Margin="5">
                                        <TextBlock Text="Chi tiết phiếu mượn:" FontWeight="SemiBold"/>
                                        <TextBlock x:Name="lblChiTietThue" TextWrapping="Wrap" Margin="0,5,0,10" Text="Vui lòng chọn một phiếu từ danh sách bên trái."/>
                                        <TextBlock Text="Tiền phạt (nếu có):" Margin="0,10,0,0"/>
                                        <TextBlock x:Name="lblTienPhat" Text="0 VND" FontSize="18" FontWeight="Bold" Foreground="{StaticResource AccentOrangeBrush}"/>
                                        <Button x:Name="btnXacNhanTraSach" Style="{StaticResource PrimaryButton}" Background="{StaticResource ActionGreenBrush}" Margin="0,15,0,0" FontWeight="Bold" Click="BtnXacNhanTraSach_Click" Height="29">
                                            <Grid>
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="Auto"/>
                                                    <ColumnDefinition Width="*"/>
                                                </Grid.ColumnDefinitions>
                                                <Viewbox Grid.Column="0" Width="14" Height="14" Margin="0,0,8,0">
                                                    <Path Data="{StaticResource IconCheck}" Fill="{Binding Foreground, RelativeSource={RelativeSource AncestorType=Button}}"/>
                                                </Viewbox>
                                                <TextBlock Grid.Column="1" Text="XÁC NHẬN TRẢ SÁCH" VerticalAlignment="Center" Margin="0,1,0,0"/>
                                            </Grid>
                                        </Button>
                                    </StackPanel>
                                </GroupBox>
                            </StackPanel>
                        </ScrollViewer>
                    </Border>
                </Grid>
            </TabItem>
        </TabControl>
    </Grid>
</Page>