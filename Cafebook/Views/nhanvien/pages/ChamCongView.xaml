<Page x:Class="Cafebook.Views.nhanvien.pages.ChamCongView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      mc:Ignorable="d" 
      d:DesignHeight="650" d:DesignWidth="1050"
      Title="ChamCongView" FontFamily="Segoe UI" Loaded="Page_Loaded" Unloaded="Page_Unloaded">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Background="WhiteSmoke" Padding="20" Margin="10" CornerRadius="5">
            <StackPanel HorizontalAlignment="Center">
                <TextBlock x:Name="lblThoiGian" Text="dd/MM/yyyy - HH:mm:ss" FontSize="32" FontWeight="Bold"/>
                <TextBlock x:Name="lblCaLamViec" Text="Đang tải lịch làm việc..." HorizontalAlignment="Center" FontSize="16" Margin="0,5,0,15"/>

                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                    <Button x:Name="btnVaoCa" Content="VÀO CA" Width="150" Height="60" FontSize="20" FontWeight="Bold"
                            Background="#27AE60" Foreground="White" Margin="10" Click="BtnVaoCa_Click"/>
                    <Button x:Name="btnRaCa" Content="RA CA" Width="150" Height="60" FontSize="20" FontWeight="Bold"
                            Background="#E74C3C" Foreground="White" Margin="10" Click="BtnRaCa_Click"/>
                </StackPanel>

                <TextBlock x:Name="lblTrangThaiChamCong" Text="Trạng thái: ..." HorizontalAlignment="Center"
                           FontSize="14" Margin="0,15,0,0" FontStyle="Italic"/>

                <Border Background="White" BorderBrush="Gainsboro" BorderThickness="1" CornerRadius="3" Padding="15" Margin="0,10,0,0" MinWidth="400">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition/>
                            <RowDefinition/>
                            <RowDefinition/>
                            <RowDefinition/>
                        </Grid.RowDefinitions>

                        <TextBlock Grid.Row="0" Grid.Column="0" Text="Giờ vào thực tế:" Margin="5" FontWeight="SemiBold"/>
                        <TextBlock Grid.Row="0" Grid.Column="1" x:Name="lblGioVaoThucTe" Text="--" Margin="5" TextAlignment="Right"/>

                        <TextBlock Grid.Row="1" Grid.Column="0" Text="Thời gian đã làm:" Margin="5" FontWeight="SemiBold"/>
                        <TextBlock Grid.Row="1" Grid.Column="1" x:Name="lblThoiGianDaLam" Text="--" Margin="5" TextAlignment="Right"/>

                        <TextBlock Grid.Row="2" Grid.Column="0" Text="Thời gian còn lại:" Margin="5" FontWeight="SemiBold"/>
                        <TextBlock Grid.Row="2" Grid.Column="1" x:Name="lblThoiGianConLai" Text="--" Margin="5" TextAlignment="Right"/>

                        <TextBlock Grid.Row="3" Grid.ColumnSpan="2" x:Name="lblCanhBao" Text="" FontSize="12" Margin="5" HorizontalAlignment="Center" Foreground="Red" FontWeight="Bold"/>
                    </Grid>
                </Border>
                <ProgressBar x:Name="pbThoiGianLamViec" Height="10" Margin="0,15,0,0"
                     Minimum="0" Maximum="100" Value="0"
                     Width="400" ToolTip="Tiến độ ca làm việc hiện tại"/>
            </StackPanel>
        </Border>

        <GroupBox Grid.Row="1" Header="Lịch sử chấm công của bạn" Margin="10">
            <DataGrid x:Name="dgLichSuChamCong" IsReadOnly="True" AutoGenerateColumns="False">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Ngày làm" Binding="{Binding NgayLam, StringFormat='dd/MM/yyyy'}" Width="*"/>
                    <DataGridTextColumn Header="Ca làm" Binding="{Binding TenCa}" Width="*"/>
                    <DataGridTextColumn Header="Giờ vào" Binding="{Binding GioVao, StringFormat='HH:mm:ss'}" Width="*"/>
                    <DataGridTextColumn Header="Giờ ra" Binding="{Binding GioRa, StringFormat='HH:mm:ss'}" Width="*"/>
                    <DataGridTextColumn Header="Tổng giờ" Binding="{Binding SoGioLam, StringFormat=N2}" Width="*"/>
                </DataGrid.Columns>
            </DataGrid>
        </GroupBox>
    </Grid>
</Page>