<Page x:Class="Cafebook.Views.admin.pages.DonHangView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:Cafebook.Views.admin.pages"
      mc:Ignorable="d" 
      d:DesignHeight="650" d:DesignWidth="1050"
      Title="DonHangView" FontFamily="Segoe UI" Loaded="Page_Loaded">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Background="WhiteSmoke" Padding="10" CornerRadius="5" Margin="0,0,0,10">
            <StackPanel Orientation="Horizontal">
                <Label Content="Từ ngày:" VerticalAlignment="Center"/>
                <DatePicker x:Name="dpTuNgay" Width="120"/>
                <Label Content="Đến ngày:" VerticalAlignment="Center" Margin="15,0,0,0"/>
                <DatePicker x:Name="dpDenNgay" Width="120"/>
                <Label Content="Trạng thái:" VerticalAlignment="Center" Margin="15,0,0,0"/>
                <ComboBox x:Name="cmbTrangThai" Width="150">
                    <ComboBoxItem Content="Tất cả"/>
                    <ComboBoxItem Content="Chưa thanh toán"/>
                    <ComboBoxItem Content="Đã thanh toán"/>
                </ComboBox>
                <Button x:Name="btnLoc" Content="Lọc" Padding="15,5" Margin="15,0,0,0" Click="BtnLoc_Click"/>
                <Button x:Name="btnXoaLoc" Content="Xóa bộ lọc" Padding="15,5" Margin="5,0,0,0" Click="BtnXoaLoc_Click"/>
            </StackPanel>
        </Border>

        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="2*"/>
                <ColumnDefinition Width="1*"/>
            </Grid.ColumnDefinitions>

            <DataGrid Grid.Column="0" x:Name="dgDonHang" AutoGenerateColumns="False" IsReadOnly="True" SelectionMode="Single" SelectionChanged="DgDonHang_SelectionChanged">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Mã HĐ" Binding="{Binding IdHoaDon}" Width="60"/>
                    <DataGridTextColumn Header="Thời gian tạo" Binding="{Binding ThoiGianTao, StringFormat='dd/MM HH:mm'}" Width="120"/>
                    <DataGridTextColumn Header="Nhân viên" Binding="{Binding TenNhanVien}" Width="*"/>
                    <DataGridTextColumn Header="Bàn" Binding="{Binding SoBan}" Width="80"/>
                    <DataGridTextColumn Header="Thành tiền" Binding="{Binding ThanhTien, StringFormat=N0}" Width="100"/>
                    <DataGridTextColumn Header="Trạng thái" Binding="{Binding TrangThai}" Width="120"/>
                </DataGrid.Columns>
            </DataGrid>

            <GroupBox Grid.Column="1" Header="Chi tiết Đơn hàng" Margin="10,0,0,0">
                <StackPanel>
                    <DataGrid x:Name="dgChiTietDonHang" AutoGenerateColumns="False" IsReadOnly="True" MinHeight="200" MaxHeight="400">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="Sản phẩm" Binding="{Binding TenSanPham}" Width="*"/>
                            <DataGridTextColumn Header="SL" Binding="{Binding SoLuong}" Width="40"/>
                            <DataGridTextColumn Header="Thành tiền" Binding="{Binding ThanhTien, StringFormat=N0}" Width="80"/>
                        </DataGrid.Columns>
                    </DataGrid>
                    <Button x:Name="btnInLaiHoaDon" Content="In lại Hóa đơn" Margin="0,10,0,0" Padding="10,5" IsEnabled="False" Click="BtnInLaiHoaDon_Click"/>
                    <Button x:Name="btnHuyDonHang" Content="Hủy Đơn hàng" Margin="0,5,0,0" Padding="10,5" IsEnabled="False" Background="IndianRed" Foreground="White" Click="BtnHuyDonHang_Click"/>
                </StackPanel>
            </GroupBox>
        </Grid>
    </Grid>
</Page>