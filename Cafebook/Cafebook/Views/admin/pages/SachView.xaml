<Page x:Class="Cafebook.Views.admin.pages.SachView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:Cafebook.Views.admin.pages"
      mc:Ignorable="d" 
      d:DesignHeight="700" d:DesignWidth="1100"
      Title="SachView" FontFamily="Segoe UI" Loaded="Page_Loaded">

    <Page.Resources>
        <SolidColorBrush x:Key="PrimaryBrownBrush" Color="#6F4E37"/>
        <SolidColorBrush x:Key="DarkBrownBrush" Color="#4E342E"/>
        <SolidColorBrush x:Key="AccentOrangeBrush" Color="#D27D2D"/>
        <SolidColorBrush x:Key="LightCreamBrush" Color="#FFF8E1"/>
        <SolidColorBrush x:Key="WhiteTextBrush" Color="#FFFFFF"/>
        <SolidColorBrush x:Key="TextGrayBrush" Color="#757575"/>
        <SolidColorBrush x:Key="BorderGrayBrush" Color="#E0E0E0"/>
        <SolidColorBrush x:Key="ActionGreenBrush" Color="#66BB6A"/>
        <SolidColorBrush x:Key="ActionRedBrush" Color="#EF5350"/>

        <Geometry x:Key="IconNew">M12,20C7.59,20 4,16.41 4,12C4,7.59 7.59,4 12,4C16.41,4 20,7.59 20,12C20,16.41 16.41,20 12,20M12,2A10,10 0 0,0 2,12A10,10 0 0,0 12,22A10,10 0 0,0 22,12A10,10 0 0,0 12,2Z</Geometry>
        <Geometry x:Key="IconAdd">M19,13H13V19H11V13H5V11H11V5H13V11H19V13Z</Geometry>
        <Geometry x:Key="IconSave">M15,9H5V5H15M12,19A3,3 0 0,1 9,16A3,3 0 0,1 12,13A3,3 0 0,1 15,16A3,3 0 0,1 12,19M17,3H5C3.89,3 3,3.9 3,5V19A2,2 0 0,0 5,21H19A2,2 0 0,0 21,19V7L17,3Z</Geometry>
        <Geometry x:Key="IconDelete">M19,4H15.5L14.5,3H9.5L8.5,4H5V6H19M6,19A2,2 0 0,0 8,21H16A2,2 0 0,0 18,19V7H6V19Z</Geometry>

        <Style x:Key="PrimaryButton" TargetType="Button">
            <Setter Property="Background" Value="{StaticResource PrimaryBrownBrush}"/>
            <Setter Property="Foreground" Value="{StaticResource WhiteTextBrush}"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Padding" Value="12,8"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" CornerRadius="4" Background="{TemplateBinding Background}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="{StaticResource DarkBrownBrush}"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Opacity" Value="0.5"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="ModernTabItem" TargetType="TabItem">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TabItem">
                        <Border x:Name="border" BorderThickness="0,0,0,3" BorderBrush="Transparent" Padding="15,10">
                            <TextBlock Text="{TemplateBinding Header}" FontSize="16" FontWeight="SemiBold" Foreground="{StaticResource TextGrayBrush}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter Property="Foreground" Value="{StaticResource PrimaryBrownBrush}"/>
                                <Setter TargetName="border" Property="BorderBrush" Value="{StaticResource AccentOrangeBrush}"/>
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Foreground" Value="{StaticResource DarkBrownBrush}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="MaterialInput" TargetType="Control">
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Padding" Value="8,5"/>
            <Setter Property="BorderBrush" Value="{StaticResource BorderGrayBrush}"/>
            <Setter Property="BorderThickness" Value="0,0,0,1"/>
            <Setter Property="Background" Value="Transparent"/>
            <Style.Triggers>
                <Trigger Property="IsKeyboardFocused" Value="True">
                    <Setter Property="BorderBrush" Value="{StaticResource PrimaryBrownBrush}"/>
                    <Setter Property="BorderThickness" Value="0,0,0,2"/>
                </Trigger>
            </Style.Triggers>
        </Style>
        <Style TargetType="TextBox" BasedOn="{StaticResource MaterialInput}"/>
        <Style TargetType="ComboBox" BasedOn="{StaticResource MaterialInput}"/>
    </Page.Resources>

    <Grid Background="{StaticResource LightCreamBrush}" x:Name="MainPanel" >
        <Grid.RenderTransform>
            <TranslateTransform Y="20"/>
        </Grid.RenderTransform>
        <Grid.Triggers>
            <EventTrigger RoutedEvent="Loaded">
                <BeginStoryboard>
                    <Storyboard>
                        <DoubleAnimation Storyboard.TargetProperty="Opacity" From="0" To="1" Duration="0:0:0.5"/>
                        <DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(TranslateTransform.Y)" To="0" Duration="0:0:0.5">
                            <DoubleAnimation.EasingFunction>
                                <BackEase EasingMode="EaseOut" Amplitude="0.2"/>
                            </DoubleAnimation.EasingFunction>
                        </DoubleAnimation>
                    </Storyboard>
                </BeginStoryboard>
            </EventTrigger>
        </Grid.Triggers>

        <TabControl BorderThickness="0" Background="Transparent" Padding="10">
            <TabControl.ItemContainerStyle>
                <Style TargetType="TabItem" BasedOn="{StaticResource ModernTabItem}"/>
            </TabControl.ItemContainerStyle>

            <TabItem Header="Quản lý Sách">
                <Grid Margin="5">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <Border Grid.Row="0" Background="{StaticResource WhiteTextBrush}" CornerRadius="8" Padding="15" Margin="0,0,0,15">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="Tìm kiếm:" VerticalAlignment="Center" FontWeight="SemiBold" Foreground="{StaticResource DarkBrownBrush}"/>
                            <TextBox x:Name="txtTimKiemSach" Width="200" Margin="10,0,0,0" TextChanged="TxtTimKiemSach_TextChanged"/>
                            <TextBlock Text="Thể loại:" VerticalAlignment="Center" FontWeight="SemiBold" Foreground="{StaticResource DarkBrownBrush}" Margin="20,0,0,0"/>
                            <ComboBox x:Name="cbFilterTheLoai" Width="150" Margin="10,0,0,0" SelectionChanged="CbFilterTheLoai_SelectionChanged"/>
                            <Button x:Name="btnLamMoiLoc" Content="Làm mới lọc" Margin="10,0,0,0" Padding="10,5" Click="BtnLamMoiLoc_Click"/>
                        </StackPanel>
                    </Border>

                    <Grid Grid.Row="1">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="380"/>
                        </Grid.ColumnDefinitions>

                        <DataGrid Grid.Column="0" x:Name="dgSach" Margin="0,0,15,0" AutoGenerateColumns="False" IsReadOnly="True" SelectionMode="Single" SelectionChanged="DgSach_SelectionChanged">
                            <DataGrid.Columns>
                                <DataGridTextColumn Header="ID" Binding="{Binding IdSach}" Width="50"/>
                                <DataGridTextColumn Header="Tiêu đề" Binding="{Binding TieuDe}" Width="*"/>
                                <DataGridTextColumn Header="Tác giả" Binding="{Binding TacGia}" Width="150"/>
                                <DataGridTextColumn Header="Thể loại" Binding="{Binding TheLoai}" Width="100"/>
                                <DataGridTextColumn Header="Vị trí" Binding="{Binding ViTri}" Width="80"/>
                                <DataGridTextColumn Header="Có sẵn" Binding="{Binding SoLuongCoSan}" Width="80"/>
                                <DataGridTextColumn Header="Tổng SL" Binding="{Binding TongSoLuong}" Width="80"/>
                            </DataGrid.Columns>
                        </DataGrid>

                        <Border Grid.Column="1" Background="{StaticResource WhiteTextBrush}" CornerRadius="8" Padding="20">
                            <ScrollViewer VerticalScrollBarVisibility="Auto">
                                <StackPanel>
                                    <TextBlock Text="Thông tin Sách" FontSize="18" FontWeight="Bold" Foreground="{StaticResource DarkBrownBrush}" Margin="0,0,0,15"/>
                                    <TextBlock Text="Tiêu đề sách:" Margin="0,0,0,5"/>
                                    <TextBox x:Name="txtTieuDe" Margin="0,0,0,10"/>
                                    <TextBlock Text="Tác giả:" Margin="0,0,0,5"/>
                                    <TextBox x:Name="txtTacGia" Margin="0,0,0,10"/>
                                    <TextBlock Text="Thể loại:" Margin="0,0,0,5"/>
                                    <TextBox x:Name="txtTheLoai" Margin="0,0,0,10"/>
                                    <TextBlock Text="Vị trí:" Margin="0,0,0,5"/>
                                    <TextBox x:Name="txtViTri" Margin="0,0,0,10"/>

                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="*"/>
                                        </Grid.ColumnDefinitions>
                                        <StackPanel Grid.Column="0" Margin="0,0,5,0">
                                            <TextBlock Text="Tổng số lượng:" Margin="0,0,0,5"/>
                                            <TextBox x:Name="txtTongSoLuong" Margin="0,0,0,10" PreviewTextInput="NumberValidationTextBox"/>
                                        </StackPanel>
                                        <StackPanel Grid.Column="1" Margin="5,0,0,0">
                                            <TextBlock Text="Giá bìa (Tiền cọc):" Margin="0,0,0,5"/>
                                            <TextBox x:Name="txtGiaBia" Margin="0,0,0,10" PreviewTextInput="NumberValidationTextBox"/>
                                        </StackPanel>
                                    </Grid>

                                    <TextBlock Text="Mô tả:" Margin="0,0,0,5"/>
                                    <TextBox x:Name="txtMoTa" Height="80" TextWrapping="Wrap" AcceptsReturn="True" Margin="0,0,0,15" VerticalScrollBarVisibility="Auto"/>

                                    <Grid Height="40">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="Auto"/>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="Auto"/>
                                            <ColumnDefinition Width="Auto"/>
                                            <ColumnDefinition Width="Auto"/>
                                        </Grid.ColumnDefinitions>
                                        <Button x:Name="btnLamMoi" Grid.Column="0" ToolTip="Làm mới" Click="BtnLamMoi_Click" Background="Transparent" BorderThickness="0" Grid.ColumnSpan="2" Margin="0,0,135,0">
                                            <Path Data="{StaticResource IconNew}" Fill="{StaticResource TextGrayBrush}" Width="22" Height="22"/>
                                        </Button>
                                        <Button x:Name="btnThem" Grid.Column="1" Style="{StaticResource PrimaryButton}" Background="{StaticResource ActionGreenBrush}" Click="BtnThem_Click" Grid.ColumnSpan="2" Margin="146,0,0,0">
                                            <Grid Width="79">
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="Auto"/>
                                                    <ColumnDefinition Width="*"/>
                                                </Grid.ColumnDefinitions>
                                                <Viewbox Grid.Column="0" Width="14" Height="14" Margin="0,0,8,0">
                                                    <Path Data="{StaticResource IconAdd}" Fill="White"/>
                                                </Viewbox>
                                                <TextBlock Text="Thêm" VerticalAlignment="Top" Margin="19,1,3,0" Grid.ColumnSpan="2"/>
                                            </Grid>
                                        </Button>
                                        <Button x:Name="btnLuu" Grid.Column="3" Style="{StaticResource PrimaryButton}" Margin="8,0,0,0" Click="BtnLuu_Click">
                                            <Grid Width="68">
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="Auto"/>
                                                    <ColumnDefinition Width="*"/>
                                                </Grid.ColumnDefinitions>
                                                <Viewbox Grid.Column="0" Width="14" Height="14" Margin="0,0,8,0">
                                                    <Path Data="{StaticResource IconSave}" Fill="White"/>
                                                </Viewbox>
                                                <TextBlock Text="Lưu" VerticalAlignment="Center" Margin="19,0,3,0" Grid.ColumnSpan="2"/>
                                            </Grid>
                                        </Button>
                                        <Button x:Name="btnXoa" Grid.Column="4" Style="{StaticResource PrimaryButton}" Background="{StaticResource ActionRedBrush}" Margin="10,0,0,0" Click="BtnXoa_Click">
                                            <Grid>
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="Auto"/>
                                                    <ColumnDefinition Width="*"/>
                                                </Grid.ColumnDefinitions>
                                                <Viewbox Grid.Column="0" Width="14" Height="14" Margin="0,0,8,0">
                                                    <Path Data="{StaticResource IconDelete}" Fill="White"/>
                                                </Viewbox>
                                                <TextBlock Text="Xóa" VerticalAlignment="Center" Margin="18,0,3,0" Grid.ColumnSpan="2"/>
                                            </Grid>
                                        </Button>
                                    </Grid>
                                </StackPanel>
                            </ScrollViewer>
                        </Border>
                    </Grid>
                </Grid>
            </TabItem>

            <TabItem Header="Lịch sử &amp; Chính sách Thuê">
                <Grid Margin="5">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <Border Grid.Row="0" Background="{StaticResource WhiteTextBrush}" CornerRadius="8" Padding="15" Margin="0,0,0,15">
                        <GroupBox Header="Sách Đang Thuê Bị Quá Hạn" Foreground="{StaticResource ActionRedBrush}" FontWeight="Bold">
                            <DataGrid x:Name="dgSachQuaHan" AutoGenerateColumns="False" IsReadOnly="True" MaxHeight="180">
                                <DataGrid.Columns>
                                    <DataGridTextColumn Header="Tiêu đề Sách" Binding="{Binding TieuDeSach}" Width="*"/>
                                    <DataGridTextColumn Header="Khách hàng" Binding="{Binding TenKhachHang}" Width="150"/>
                                    <DataGridTextColumn Header="Ngày hẹn trả" Binding="{Binding NgayHenTra, StringFormat='dd/MM/yyyy'}" Width="100"/>
                                    <DataGridTextColumn Header="Trạng thái" Binding="{Binding TrangThai}" Width="100"/>
                                </DataGrid.Columns>
                            </DataGrid>
                        </GroupBox>
                    </Border>

                    <Border Grid.Row="1" Background="{StaticResource WhiteTextBrush}" CornerRadius="8" Padding="15" Margin="0,0,0,15">
                        <GroupBox Header="Thiết lập Chính sách Cho thuê">
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="Phí thuê mỗi lượt (VND):" VerticalAlignment="Center" Margin="0,0,5,0"/>
                                <TextBox x:Name="txtPhiThue" Width="100" Margin="0,0,20,0" PreviewTextInput="NumberValidationTextBox"/>
                                <TextBlock Text="Phí phạt mỗi ngày trễ hạn (VND):" VerticalAlignment="Center" Margin="0,0,5,0"/>
                                <TextBox x:Name="txtPhiPhat" Width="100" Margin="0,0,20,0" PreviewTextInput="NumberValidationTextBox"/>
                                <Button x:Name="btnLuuChinhSach" Style="{StaticResource PrimaryButton}" Click="BtnLuuChinhSach_Click" Height="38">
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="Auto"/>
                                            <ColumnDefinition Width="*"/>
                                        </Grid.ColumnDefinitions>
                                        <Viewbox Grid.Column="0" Width="14" Height="14" Margin="0,0,8,0">
                                            <Path Data="{StaticResource IconSave}" Fill="White"/>
                                        </Viewbox>
                                        <TextBlock Text="Lưu Chính sách" VerticalAlignment="Center" Margin="19,0,3,0" Grid.ColumnSpan="2"/>
                                    </Grid>
                                </Button>
                            </StackPanel>
                        </GroupBox>
                    </Border>

                    <GroupBox Grid.Row="2" Header="Lịch sử Cho thuê Sách">
                        <DataGrid x:Name="dgPhieuThueSach" AutoGenerateColumns="False" IsReadOnly="True">
                            <DataGrid.Columns>
                                <DataGridTextColumn Header="Tiêu đề Sách" Binding="{Binding TieuDeSach}" Width="*"/>
                                <DataGridTextColumn Header="Khách hàng" Binding="{Binding TenKhachHang}" Width="150"/>
                                <DataGridTextColumn Header="Ngày thuê" Binding="{Binding NgayThue, StringFormat='dd/MM/yyyy'}" Width="100"/>
                                <DataGridTextColumn Header="Hẹn trả" Binding="{Binding NgayHenTra, StringFormat='dd/MM/yyyy'}" Width="100"/>
                                <DataGridTextColumn Header="Trả thực tế" Binding="{Binding NgayTraThucTe, StringFormat='dd/MM/yyyy'}" Width="100"/>
                                <DataGridTextColumn Header="Tiền phạt" Binding="{Binding TienPhat, StringFormat=N0}" Width="100"/>
                                <DataGridTextColumn Header="Trạng thái" Binding="{Binding TrangThai}" Width="100"/>
                            </DataGrid.Columns>
                        </DataGrid>
                    </GroupBox>
                </Grid>
            </TabItem>
        </TabControl>
    </Grid>
</Page>