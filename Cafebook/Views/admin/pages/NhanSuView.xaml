<Page x:Class="Cafebook.Views.admin.pages.NhanSuView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      mc:Ignorable="d" 
      d:DesignHeight="700" d:DesignWidth="1100"
      Title="NhanSuView" FontFamily="Segoe UI" Loaded="Page_Loaded">

    <Page.Resources>
        <SolidColorBrush x:Key="PrimaryBrownBrush" Color="#6F4E37"/>
        <SolidColorBrush x:Key="DarkBrownBrush" Color="#4E342E"/>
        <SolidColorBrush x:Key="AccentOrangeBrush" Color="#D27D2D"/>
        <SolidColorBrush x:Key="LightCreamBrush" Color="#FFF8E1"/>
        <SolidColorBrush x:Key="WhiteTextBrush" Color="#FFFFFF"/>
        <SolidColorBrush x:Key="TextGrayBrush" Color="#757575"/>
        <SolidColorBrush x:Key="BorderGrayBrush" Color="#E0E0E0"/>
        <SolidColorBrush x:Key="ActionGreenBrush" Color="#66BB6A"/>
        <SolidColorBrush x:Key="ActionRedBrush" Color="#EF5350"/>

        <Geometry x:Key="IconNew">M12,20C7.59,20 4,16.41 4,12C4,7.59 7.59,4 12,4C16.41,4 20,7.59 20,12C20,16.41 16.41,20 12,20M12,2A10,10 0 0,0 2,12A10,10 0 0,0 12,22A10,10 0 0,0 22,12A10,10 0 0,0 12,2Z</Geometry>
        <Geometry x:Key="IconAdd">M19,13H13V19H11V13H5V11H11V5H13V11H19V13Z</Geometry>
        <Geometry x:Key="IconSave">M15,9H5V5H15M12,19A3,3 0 0,1 9,16A3,3 0 0,1 12,13A3,3 0 0,1 15,16A3,3 0 0,1 12,19M17,3H5C3.89,3 3,3.9 3,5V19A2,2 0 0,0 5,21H19A2,2 0 0,0 21,19V7L17,3Z</Geometry>
        <Geometry x:Key="IconDelete">M19,4H15.5L14.5,3H9.5L8.5,4H5V6H19M6,19A2,2 0 0,0 8,21H16A2,2 0 0,0 18,19V7H6V19Z</Geometry>

        <Style x:Key="PrimaryButton" TargetType="Button">
            <Setter Property="Background" Value="{StaticResource PrimaryBrownBrush}"/>
            <Setter Property="Foreground" Value="{StaticResource WhiteTextBrush}"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Padding" Value="12,8"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" CornerRadius="4" Background="{TemplateBinding Background}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="{StaticResource DarkBrownBrush}"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Opacity" Value="0.5"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="ModernTabItem" TargetType="TabItem">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TabItem">
                        <Border x:Name="border" BorderThickness="0,0,0,3" BorderBrush="Transparent" Padding="15,10">
                            <TextBlock Text="{TemplateBinding Header}" FontSize="16" FontWeight="SemiBold" Foreground="{StaticResource TextGrayBrush}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter Property="Foreground" Value="{StaticResource PrimaryBrownBrush}"/>
                                <Setter TargetName="border" Property="BorderBrush" Value="{StaticResource AccentOrangeBrush}"/>
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Foreground" Value="{StaticResource DarkBrownBrush}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="MaterialInput" TargetType="Control">
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Padding" Value="8,5"/>
            <Setter Property="BorderBrush" Value="{StaticResource BorderGrayBrush}"/>
            <Setter Property="BorderThickness" Value="0,0,0,1"/>
            <Setter Property="Background" Value="Transparent"/>
            <Style.Triggers>
                <Trigger Property="IsKeyboardFocused" Value="True">
                    <Setter Property="BorderBrush" Value="{StaticResource PrimaryBrownBrush}"/>
                    <Setter Property="BorderThickness" Value="0,0,0,2"/>
                </Trigger>
            </Style.Triggers>
        </Style>
        <Style TargetType="TextBox" BasedOn="{StaticResource MaterialInput}"/>
        <Style TargetType="PasswordBox" BasedOn="{StaticResource MaterialInput}"/>
        <Style TargetType="ComboBox" BasedOn="{StaticResource MaterialInput}"/>
        <Style TargetType="DatePicker" BasedOn="{StaticResource MaterialInput}"/>
    </Page.Resources>

    <Grid Background="{StaticResource LightCreamBrush}" x:Name="MainPanel" >
        <Grid.RenderTransform>
            <TranslateTransform Y="20"/>
        </Grid.RenderTransform>
        <Grid.Triggers>
            <EventTrigger RoutedEvent="Loaded">
                <BeginStoryboard>
                    <Storyboard>
                        <DoubleAnimation Storyboard.TargetProperty="Opacity" From="0" To="1" Duration="0:0:0.5"/>
                        <DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(TranslateTransform.Y)" To="0" Duration="0:0:0.5">
                            <DoubleAnimation.EasingFunction>
                                <BackEase EasingMode="EaseOut" Amplitude="0.2"/>
                            </DoubleAnimation.EasingFunction>
                        </DoubleAnimation>
                    </Storyboard>
                </BeginStoryboard>
            </EventTrigger>
        </Grid.Triggers>

        <TabControl BorderThickness="0" Background="Transparent" Padding="10">
            <TabControl.ItemContainerStyle>
                <Style TargetType="TabItem" BasedOn="{StaticResource ModernTabItem}"/>
            </TabControl.ItemContainerStyle>

            <TabItem Header="Danh sách Nhân viên">
                <Grid Margin="5">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="380"/>
                    </Grid.ColumnDefinitions>
                    <DataGrid Grid.Column="0" x:Name="dgNhanVien" Margin="0,0,15,0" AutoGenerateColumns="False" IsReadOnly="True" SelectionMode="Single" SelectionChanged="DgNhanVien_SelectionChanged">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="Họ Tên" Binding="{Binding HoTen}" Width="*"/>
                            <DataGridTextColumn Header="Vai trò" Binding="{Binding TenVaiTro}" Width="120"/>
                            <DataGridTextColumn Header="Lương/giờ" Binding="{Binding MucLuongTheoGio, StringFormat=N0}" Width="100"/>
                            <DataGridCheckBoxColumn Header="Đang làm" Binding="{Binding TrangThai}" Width="80"/>
                        </DataGrid.Columns>
                    </DataGrid>

                    <Border Grid.Column="1" Background="{StaticResource WhiteTextBrush}" CornerRadius="8" Padding="20">
                        <ScrollViewer VerticalScrollBarVisibility="Auto">
                            <StackPanel>
                                <TextBlock Text="Thông tin chi tiết" FontSize="18" FontWeight="Bold" Foreground="{StaticResource DarkBrownBrush}" Margin="0,0,0,15"/>
                                <TextBlock Text="Họ tên:" Margin="0,0,0,5"/>
                                <TextBox x:Name="txtHoTen" Margin="0,0,0,10"/>
                                <TextBlock Text="Mật khẩu (để trống nếu không đổi):" Margin="0,0,0,5"/>
                                <PasswordBox x:Name="txtMatKhau" Margin="0,0,0,10"/>
                                <TextBlock Text="Mức lương/giờ (VND):" Margin="0,0,0,5"/>
                                <TextBox x:Name="txtMucLuong" Margin="0,0,0,10"/>
                                <TextBlock Text="Vai trò:" Margin="0,0,0,5"/>
                                <ComboBox x:Name="cmbVaiTro" DisplayMemberPath="TenVaiTro" SelectedValuePath="IdVaiTro" Margin="0,0,0,10"/>
                                <CheckBox x:Name="chkTrangThai" Content="Đang làm việc" Margin="0,5,0,15"/>

                                <Grid Height="40">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>
                                    <Button x:Name="btnLamMoiNV" Grid.Column="0" ToolTip="Làm mới" Click="BtnLamMoiNV_Click" Background="Transparent" BorderThickness="0" Grid.ColumnSpan="2" Margin="0,0,188,0">
                                        <Path Data="{StaticResource IconNew}" Fill="{StaticResource TextGrayBrush}" Width="22" Height="22"/>
                                    </Button>
                                    <Button x:Name="btnThemNV" Grid.Column="1" Style="{StaticResource PrimaryButton}" Click="BtnThemNV_Click" Background="{StaticResource ActionGreenBrush}" Margin="181,0,0,0">
                                        <Grid>
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="Auto"/>
                                                <ColumnDefinition Width="*"/>
                                            </Grid.ColumnDefinitions>
                                            <Viewbox Grid.Column="0" Width="14" Height="14" Margin="0,0,8,0">
                                                <Path Data="{StaticResource IconAdd}" Fill="White"/>
                                            </Viewbox>
                                            <TextBlock Text="Thêm" VerticalAlignment="Center" Grid.ColumnSpan="2" Margin="19,0,3,0"/>
                                        </Grid>
                                    </Button>
                                    <Button x:Name="btnLuuNV" Grid.Column="3" Style="{StaticResource PrimaryButton}" Margin="8,0,0,0" Click="BtnLuuNV_Click">
                                        <Grid Width="70">
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="Auto"/>
                                                <ColumnDefinition Width="*"/>
                                            </Grid.ColumnDefinitions>
                                            <Viewbox Grid.Column="0" Width="14" Height="14" Margin="0,0,8,0">
                                                <Path Data="{StaticResource IconSave}" Fill="White"/>
                                            </Viewbox>
                                            <TextBlock Text="Lưu" VerticalAlignment="Center" Grid.ColumnSpan="2" Margin="19,0,3,0"/>
                                        </Grid>
                                    </Button>
                                </Grid>
                            </StackPanel>
                        </ScrollViewer>
                    </Border>
                </Grid>
            </TabItem>

            <TabItem Header="Lịch làm việc">
                <Grid Margin="10">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <StackPanel Grid.Column="0">
                        <Calendar x:Name="calLichLamViec" SelectedDatesChanged="CalLichLamViec_SelectedDatesChanged"/>
                        <Border Background="{StaticResource WhiteTextBrush}" CornerRadius="8" Padding="15" Margin="0,10,0,0">
                            <StackPanel>
                                <TextBlock Text="Thêm/Sửa lịch làm việc" FontSize="16" FontWeight="SemiBold" Margin="0,0,0,10"/>
                                <TextBlock Text="Chọn nhân viên:" Margin="0,0,0,5"/>
                                <ComboBox x:Name="cmbNhanVien_Lich" DisplayMemberPath="HoTen" SelectedValuePath="IdNhanVien" Margin="0,0,0,10"/>
                                <TextBlock Text="Chọn ca làm việc mẫu (tùy chọn):" Margin="0,0,0,5"/>
                                <ComboBox x:Name="cmbCaLamViec_Mau" DisplayMemberPath="TenCa" Margin="0,0,0,10" SelectionChanged="CmbCaLamViec_Mau_SelectionChanged" ToolTip="Chọn một ca mẫu sẽ tự động điền giờ bắt đầu và kết thúc."/>
                                <TextBlock Text="Giờ bắt đầu (HH:mm):" Margin="0,0,0,5"/>
                                <TextBox x:Name="txtGioBatDau" Text="07:00" Margin="0,0,0,10"/>
                                <TextBlock Text="Giờ kết thúc (HH:mm):" Margin="0,0,0,5"/>
                                <TextBox x:Name="txtGioKetThuc" Text="15:00" Margin="0,0,0,10"/>
                                <TextBlock Text="Trạng thái:" Margin="0,0,0,5"/>
                                <ComboBox x:Name="cmbTrangThaiLich" SelectedIndex="0" Margin="0,0,0,15">
                                    <ComboBoxItem Content="Đi Làm"/>
                                    <ComboBoxItem Content="Nghỉ Có Phép"/>
                                    <ComboBoxItem Content="Nghỉ Không Phép"/>
                                </ComboBox>
                                <Button x:Name="btnThemLich" Content="Thêm Lịch" Style="{StaticResource PrimaryButton}" Click="BtnThemLich_Click" Height="40"/>
                            </StackPanel>
                        </Border>
                    </StackPanel>

                    <Grid Grid.Column="1" Margin="10,0,0,0">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>
                        <Border Background="{StaticResource WhiteTextBrush}" CornerRadius="8" Padding="15" Margin="0,0,0,10">
                            <StackPanel Orientation="Horizontal">
                                <TextBlock x:Name="lblNgayDaChon" Text="Lịch làm việc ngày: " FontSize="18" FontWeight="SemiBold" VerticalAlignment="Center" Foreground="{StaticResource DarkBrownBrush}"/>
                                <Button x:Name="btnXoaLich" Style="{StaticResource PrimaryButton}" Background="{StaticResource ActionRedBrush}" Margin="20,0,0,0" Click="BtnXoaLich_Click" Height="35">
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="Auto"/>
                                            <ColumnDefinition Width="*"/>
                                        </Grid.ColumnDefinitions>
                                        <Viewbox Grid.Column="0" Width="14" Height="14" Margin="0,0,8,0">
                                            <Path Data="{StaticResource IconDelete}" Fill="White"/>
                                        </Viewbox>
                                        <TextBlock Grid.Column="1" Text="Xóa Lịch đã chọn" VerticalAlignment="Center"/>
                                    </Grid>
                                </Button>
                            </StackPanel>
                        </Border>
                        <DataGrid Grid.Row="1" x:Name="dgLichLamViec" AutoGenerateColumns="False" IsReadOnly="True" SelectionMode="Single" SelectedValuePath="IdLichLamViec">
                            <DataGrid.Columns>
                                <DataGridTextColumn Header="Nhân viên" Binding="{Binding HoTenNhanVien}" Width="*"/>
                                <DataGridTextColumn Header="Bắt đầu" Binding="{Binding GioBatDau, StringFormat=hh\\:mm}" Width="100"/>
                                <DataGridTextColumn Header="Kết thúc" Binding="{Binding GioKetThuc, StringFormat=hh\\:mm}" Width="100"/>
                                <DataGridTextColumn Header="Trạng thái" Binding="{Binding TrangThai}" Width="120"/>
                            </DataGrid.Columns>
                        </DataGrid>
                    </Grid>
                </Grid>
            </TabItem>

            <TabItem Header="Thiết lập Lương &amp; Chính sách">
                <Grid Margin="5">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="380"/>
                    </Grid.ColumnDefinitions>

                    <DataGrid Grid.Column="0" x:Name="dgQuyTacLuong" Margin="0,0,15,0" AutoGenerateColumns="False" IsReadOnly="True" SelectionMode="Single" SelectionChanged="DgQuyTacLuong_SelectionChanged">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="Tên Quy Tắc" Binding="{Binding TenQuyTac}" Width="*"/>
                            <DataGridTextColumn Header="Loại Quy Tắc" Binding="{Binding LoaiQuyTac}" Width="120"/>
                            <DataGridTextColumn Header="Giá trị" Binding="{Binding GiaTriApDung, StringFormat=N2}" Width="100"/>
                            <DataGridTextColumn Header="Đơn vị" Binding="{Binding DonViTinh}" Width="100"/>
                            <DataGridTextColumn Header="Phân Loại" Binding="{Binding Loai}" Width="100"/>
                        </DataGrid.Columns>
                    </DataGrid>

                    <Border Grid.Column="1" Background="{StaticResource WhiteTextBrush}" CornerRadius="8" Padding="20">
                        <ScrollViewer VerticalScrollBarVisibility="Auto">
                            <StackPanel>
                                <TextBlock Text="Thêm/Sửa Quy tắc lương" FontSize="18" FontWeight="Bold" Foreground="{StaticResource DarkBrownBrush}" Margin="0,0,0,15"/>
                                <TextBlock Text="Tên quy tắc (vd: Phạt đi trễ):" Margin="0,0,0,5"/>
                                <TextBox x:Name="txtTenQuyTac" Margin="0,0,0,10"/>
                                <TextBlock Text="Loại quy tắc:" Margin="0,0,0,5"/>
                                <ComboBox x:Name="cmbLoaiQuyTac" SelectedIndex="0" Margin="0,0,0,10" SelectionChanged="CmbLoaiQuyTac_SelectionChanged">
                                    <ComboBoxItem Content="Cố định" Tag="FLAT"/>
                                    <ComboBoxItem Content="Đi trễ" Tag="LATE"/>
                                    <ComboBoxItem Content="Tăng ca" Tag="OVERTIME"/>
                                    <ComboBoxItem Content="Thưởng theo giờ/tháng" Tag="MONTHLY_HOURS"/>
                                </ComboBox>
                                <TextBlock Text="Điều kiện:" Margin="0,0,0,5"/>
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>
                                    <TextBox Grid.Column="0" x:Name="txtDieuKien" Margin="0,0,5,10"/>
                                    <ComboBox Grid.Column="1" x:Name="cmbDieuKienDonViTinh" Margin="0,0,0,10" Width="80">
                                        <ComboBoxItem>Phút</ComboBoxItem>
                                        <ComboBoxItem>Giờ</ComboBoxItem>
                                    </ComboBox>
                                </Grid>
                                <TextBlock Text="Giá trị áp dụng (vd: 50000 hoặc -1.5):" Margin="0,0,0,5"/>
                                <TextBox x:Name="txtGiaTriApDung" Margin="0,0,0,10"/>
                                <TextBlock Text="Đơn vị tính:" Margin="0,0,0,5"/>
                                <ComboBox x:Name="cmbDonViTinh" SelectedIndex="0" Margin="0,0,0,15">
                                    <ComboBoxItem Content="VND" Tag="VND"/>
                                    <ComboBoxItem Content="Hệ số nhân" Tag="Multiplier"/>
                                </ComboBox>
                                <TextBlock Text="Phân loại (Thưởng/Phạt):" Margin="0,0,0,5"/>
                                <ComboBox x:Name="cmbLoaiThuongPhat" SelectedIndex="0" Margin="0,0,0,15">
                                    <ComboBoxItem Content="Thuong"/>
                                    <ComboBoxItem Content="Phat"/>
                                </ComboBox>
                                <Grid Height="40">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>
                                    <Button x:Name="btnLamMoiQT" Grid.Column="0" ToolTip="Làm mới" Click="BtnLamMoiQT_Click" Background="Transparent" BorderThickness="0" Grid.ColumnSpan="2" Margin="0,0,134,0">
                                        <Path Data="{StaticResource IconNew}" Fill="{StaticResource TextGrayBrush}" Width="22" Height="22"/>
                                    </Button>
                                    <Button x:Name="btnThemQT" Grid.Column="1" Style="{StaticResource PrimaryButton}" Background="{StaticResource ActionGreenBrush}" Click="BtnThemQT_Click" Grid.ColumnSpan="2" Margin="148,0,0,0">
                                        <Grid>
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="Auto"/>
                                                <ColumnDefinition Width="*"/>
                                            </Grid.ColumnDefinitions>
                                            <Viewbox Grid.Column="0" Width="14" Height="14" Margin="0,0,8,0">
                                                <Path Data="{StaticResource IconAdd}" Fill="White"/>
                                            </Viewbox>
                                            <TextBlock Text="Thêm" VerticalAlignment="Center" Margin="18,0,5,0" Grid.ColumnSpan="2"/>
                                        </Grid>
                                    </Button>
                                    <Button x:Name="btnLuuQT" Grid.Column="3" Style="{StaticResource PrimaryButton}" Margin="8,0,0,0" Click="BtnLuuQT_Click">
                                        <Grid>
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="Auto"/>
                                                <ColumnDefinition Width="*"/>
                                            </Grid.ColumnDefinitions>
                                            <Viewbox Grid.Column="0" Width="14" Height="14" Margin="0,0,8,0">
                                                <Path Data="{StaticResource IconSave}" Fill="White"/>
                                            </Viewbox>
                                            <TextBlock Text="Lưu" VerticalAlignment="Top" Margin="19,2,3,0" Grid.ColumnSpan="2"/>
                                        </Grid>
                                    </Button>
                                    <Button x:Name="btnXoaQT" Grid.Column="4" Style="{StaticResource PrimaryButton}" Background="{StaticResource ActionRedBrush}" Margin="11,0,0,0" Click="BtnXoaQT_Click">
                                        <Grid>
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="Auto"/>
                                                <ColumnDefinition Width="*"/>
                                            </Grid.ColumnDefinitions>
                                            <Viewbox Grid.Column="0" Width="14" Height="14" Margin="0,0,8,0">
                                                <Path Data="{StaticResource IconDelete}" Fill="White"/>
                                            </Viewbox>
                                            <TextBlock Text="Xóa" VerticalAlignment="Center" Margin="18,0,3,0" Grid.ColumnSpan="2"/>
                                        </Grid>
                                    </Button>
                                </Grid>
                            </StackPanel>
                        </ScrollViewer>
                    </Border>
                </Grid>
            </TabItem>

            <TabItem Header="Chấm công &amp; Phiếu lương">
                <Grid Margin="5">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="2*"/>
                        <ColumnDefinition Width="3*"/>
                    </Grid.ColumnDefinitions>

                    <Border Grid.Column="0" Background="{StaticResource WhiteTextBrush}" CornerRadius="8" Padding="15" Margin="0,0,15,0">
                        <StackPanel>
                            <TextBlock Text="Bảng chấm công chi tiết" FontSize="16" FontWeight="SemiBold" Margin="0,0,0,10"/>
                            <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
                                <TextBlock Text="Xem ngày:" VerticalAlignment="Center"/>
                                <DatePicker x:Name="dpNgayChamCong" Margin="10,0,0,0" SelectedDateChanged="DpNgayChamCong_SelectedDateChanged"/>
                            </StackPanel>
                            <DataGrid x:Name="dgChamCong" AutoGenerateColumns="False" IsReadOnly="True">
                                <DataGrid.Columns>
                                    <DataGridTextColumn Header="Nhân viên" Binding="{Binding HoTenNhanVien}" Width="*"/>
                                    <DataGridTextColumn Header="Giờ vào" Binding="{Binding GioVao, StringFormat='HH:mm:ss'}" Width="100"/>
                                    <DataGridTextColumn Header="Giờ ra" Binding="{Binding GioRa, StringFormat='HH:mm:ss'}" Width="100"/>
                                    <DataGridTextColumn Header="Tổng giờ" Binding="{Binding SoGioLam, StringFormat=N2}" Width="100"/>
                                </DataGrid.Columns>
                            </DataGrid>
                        </StackPanel>
                    </Border>

                    <ScrollViewer Grid.Column="1" VerticalScrollBarVisibility="Auto">
                        <StackPanel>
                            <Border Background="{StaticResource WhiteTextBrush}" CornerRadius="8" Padding="20">
                                <StackPanel>
                                    <TextBlock Text="Tính và Chốt Lương" FontSize="18" FontWeight="Bold" Foreground="{StaticResource DarkBrownBrush}" Margin="0,0,0,15"/>
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="*"/>
                                        </Grid.ColumnDefinitions>
                                        <StackPanel Grid.Column="0" Margin="0,0,10,0">
                                            <TextBlock Text="Chọn nhân viên:" Margin="0,0,0,5"/>
                                            <ComboBox x:Name="cmbNhanVien_Luong" DisplayMemberPath="HoTen" SelectedValuePath="IdNhanVien" SelectionChanged="CmbNhanVien_Luong_SelectionChanged"/>
                                            <TextBlock Text="Từ ngày:" Margin="0,10,0,5"/>
                                            <DatePicker x:Name="dpTuNgay" SelectedDateChanged="DpLuong_SelectedDateChanged"/>
                                            <TextBlock Text="Đến ngày:" Margin="0,10,0,5"/>
                                            <DatePicker x:Name="dpDenNgay" SelectedDateChanged="DpLuong_SelectedDateChanged"/>
                                            <Button Content="TÍNH LƯƠNG TẠM TÍNH" Margin="0,15,0,0" Style="{StaticResource PrimaryButton}" Click="BtnTinhLuong_Click" Height="40"/>
                                        </StackPanel>

                                        <Border Grid.Column="1" Margin="10,0,0,0" Background="{StaticResource LightCreamBrush}" Padding="10" CornerRadius="5">
                                            <StackPanel>
                                                <TextBlock Text="CHI TIẾT LƯƠNG" FontWeight="Bold" FontSize="14" HorizontalAlignment="Center" Margin="0,0,0,10"/>
                                                <TextBlock>Lương theo giờ: <Run x:Name="runLuongTheoGio" FontWeight="SemiBold"/></TextBlock>
                                                <TextBlock>Tổng giờ làm: <Run x:Name="runTongGioLam" FontWeight="SemiBold"/></TextBlock>
                                                <TextBlock>Tổng thưởng: <Run x:Name="runTongThuong" FontWeight="SemiBold" Foreground="Green"/></TextBlock>
                                                <TextBlock>Tổng phạt: <Run x:Name="runTongPhat" FontWeight="SemiBold" Foreground="Red"/></TextBlock>
                                                <Separator Margin="0,5"/>
                                                <TextBlock FontSize="16" FontWeight="Bold">Thực lãnh: <Run x:Name="runThucLanh" Foreground="Blue"/></TextBlock>
                                            </StackPanel>
                                        </Border>
                                    </Grid>
                                </StackPanel>
                            </Border>

                            <Border Background="{StaticResource WhiteTextBrush}" CornerRadius="8" Padding="20" Margin="0,15,0,0">
                                <StackPanel>
                                    <TextBlock Text="Thêm Thưởng/Phạt thủ công" FontSize="16" FontWeight="SemiBold" Margin="0,0,0,10"/>
                                    <TextBlock Text="Chọn từ quy tắc có sẵn (tùy chọn):" Margin="0,0,0,5"/>
                                    <ComboBox x:Name="cmbQuyTac_Mau" DisplayMemberPath="TenQuyTac" Margin="0,0,0,10" SelectionChanged="CmbQuyTac_Mau_SelectionChanged"/>
                                    <TextBlock Text="Nội dung (có thể sửa lại):" Margin="0,0,0,5"/>
                                    <TextBox x:Name="txtNoiDungPhat" Margin="0,0,0,10"/>
                                    <TextBlock Text="Số tiền (nhập số âm nếu là phạt):" Margin="0,0,0,5"/>
                                    <TextBox x:Name="txtSoTienPhat" Margin="0,0,0,10"/>
                                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                                        <Button x:Name="btnLamMoiTPTC" Content="Làm mới" Margin="5,0" Click="BtnLamMoiTPTC_Click" Width="83"/>
                                        <Button x:Name="btnLuuTPTC" Content="Lưu" Style="{StaticResource PrimaryButton}" Click="BtnLuuTPTC_Click" Width="40" Height="40"/>
                                    </StackPanel>
                                </StackPanel>
                            </Border>

                            <Border Background="{StaticResource WhiteTextBrush}" CornerRadius="8" Padding="20" Margin="0,15,0,0">
                                <StackPanel>
                                    <TextBlock Text="Danh sách Thưởng/Phạt trong kỳ" FontSize="16" FontWeight="SemiBold" Margin="0,0,0,10"/>
                                    <DataGrid x:Name="dgThuongPhatThuCong" AutoGenerateColumns="False" IsReadOnly="True" MaxHeight="150" SelectionChanged="DgThuongPhatThuCong_SelectionChanged">
                                        <DataGrid.Columns>
                                            <DataGridTextColumn Header="Nội dung" Binding="{Binding GhiChu}" Width="*"/>
                                            <DataGridTextColumn Header="Số tiền" Binding="{Binding SoTien, StringFormat=N0}" Width="120"/>
                                        </DataGrid.Columns>
                                    </DataGrid>
                                    <Button x:Name="btnXoaTPTC" Content="Xóa Khoản Đã Chọn" HorizontalAlignment="Right" Margin="0,10,0,0" Style="{StaticResource PrimaryButton}" Background="{StaticResource ActionRedBrush}" Click="BtnXoaTPTC_Click"/>
                                </StackPanel>
                            </Border>

                            <Button x:Name="btnChotLuong" Content="CHỐT LƯƠNG VÀ TẠO PHIẾU LƯƠNG" Margin="0,15,0,0" Padding="10" FontWeight="Bold" Background="{StaticResource ActionGreenBrush}" Foreground="White" IsEnabled="False" Click="BtnChotLuong_Click"/>

                            <DataGrid x:Name="dgPhieuLuong" Margin="0,10,0,0" IsReadOnly="True" AutoGenerateColumns="False" MaxHeight="200" ToolTip="Lịch sử các phiếu lương đã chốt của nhân viên được chọn">
                                <DataGrid.Columns>
                                    <DataGridTextColumn Header="Kỳ Lương" Binding="{Binding TuNgay, StringFormat='{}{0:MM/yyyy}'}" Width="*"/>
                                    <DataGridTextColumn Header="Thực Lãnh" Binding="{Binding ThucLanh, StringFormat=N0}" Width="120"/>
                                    <DataGridTextColumn Header="Ngày Chốt" Binding="{Binding NgayTinhLuong, StringFormat='dd/MM/yyyy'}" Width="120"/>
                                    <DataGridTemplateColumn Width="Auto">
                                        <DataGridTemplateColumn.CellTemplate>
                                            <DataTemplate>
                                                <Button Content="Xem" Margin="5,2" Click="BtnXemPhieuLuong_Click"/>
                                            </DataTemplate>
                                        </DataGridTemplateColumn.CellTemplate>
                                    </DataGridTemplateColumn>
                                </DataGrid.Columns>
                            </DataGrid>
                        </StackPanel>
                    </ScrollViewer>
                </Grid>
            </TabItem>
        </TabControl>
    </Grid>
</Page>